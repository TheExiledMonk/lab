# Native Forward Transfer Operator Audit 001

## Question

Can the already-frozen A8 pair-transfer + PM1/PS2 + M10 machinery generate the missing local propagation-interface scale directly from the native bounded-strain accumulated equilibrium, without multiplying frozen factors together by inspection?

## Forward diagnostic construction

The native bridge remains:

```text
rho3
 -> zero-flux raw c_state
 -> bounded-strain accumulated equilibrium u_accum
```

For this audit only, one explicit diagnostic embedding is tested:

```text
u_fast = u_slow = u_accum
```

The exact existing A8 pair law is then applied unchanged:

```text
A_ij = (dt*omega*K) * Delta u_fast
     + (dt*tau_slow) * Delta u_slow
```

followed by the existing:

```text
PM1 / PS2 pair response
 -> M10 midpoint rasterisation
```

No extra coupling factor is multiplied in by hand. In particular, the `0.5` fast/slow coupling constants only count if they enter through the actual frozen equations being executed.

## Why this matters

PR #88 found that hand-combined frozen factors could land near the inverse-source transfer scale, especially `0.00825`. This audit asks whether that factor is actually generated by the forward machinery. If not, the near-match remains only a numerical clue rather than a derived bridge.

## Guardrails

- five canonical local `PBUF_benchmark` FITS only;
- canonical native zero-flux accumulation preserved;
- no network access;
- no observed lensing values;
- no historical `strength=0.18` in the native route;
- no replacement strength scalar;
- no normalization or rescaling;
- no fitting or tuning;
- no hand-applied coupling or midpoint coefficient beyond what existing PM1/PS2/M10 code performs;
- no candidate fed into G3D;
- no GR/Weyl/LCDM/Rmax/QE/Planck input.

## Run

```bash
PYTHONPATH=. python pbuf/labs/foundation/native_forward_transfer_operator_audit001.py
```

## Status values

- `NATIVE_FORWARD_TRANSFER_OPERATOR_AUDIT_EXECUTED`
- `NATIVE_FORWARD_TRANSFER_OPERATOR_AUDIT_PARTIAL_EXECUTION`
- `NATIVE_FORWARD_TRANSFER_OPERATOR_AUDIT_NOT_ESTABLISHED`

This is an operator audit only. The equal-channel embedding is an explicit diagnostic assumption and is not promoted to a physical law by this PR.
