# Spatial / Coherence Kernel Audit 001

Fact-finding runner note only.

Run from repository root after merge:

```bash
PYTHONPATH=. python pbuf/labs/foundation/spatial_coherence_kernel_audit001.py
```

Return the current HEAD SHA, exit code, complete raw stdout, and `git status --short` after the run.

Do not modify, repair, reinterpret, optimize, or rerun with changed inputs if the lab fails. Do not create or edit files. Do not pop or remove any preservation stash.

Scientific scope: mathematical target-blind spatial/coherence weighting families only. No kernel is selected or promoted to PBUF physics. No kappa, shear, HST target, lens benchmark, GR/Newtonian calibration, Quantum Engine input, or fitting is authorized.
