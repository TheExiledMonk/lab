{
  "UNLOADED_CONTROL_REUSED": true,
  "UNLOADED_SPATIAL_WINDING": "ZERO",
  "reason": "existing unloaded geometry is u=0, so every required bond force is exactly zero and normalized circulation is undefined; raw existing force is zero."
}
